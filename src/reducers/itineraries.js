// import { ITINERARIES_FETCH } from '../constants';

export default function itineraries(state = {}, action) {
  switch (action.type) {
    //  case ITINERARIES_FETCH:
    //  return {
    //    ...state,
    //    [action.payload.name]: action.payload.value,
    //  };
    default:
      return state;
  }
}
